# SVM Baseline (BRISC2025)

This folder contains the SVM notebook for 4-class brain tumor classification.

## Notebook
- `multi_class_svm.ipynb`

## Notes
- Uses HOG + PCA + SVM pipeline.
- Label metadata is stored in `data/labels/`.
- Update dataset paths in notebook cells before execution.
